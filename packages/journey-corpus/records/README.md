# Platform records

Each file in this directory is the canonical result of one independent
organization-specific research assignment. Filenames must match the roster
slug. Do not create prose summaries outside the record; the catalog is generated
from these JSON files after validation.
